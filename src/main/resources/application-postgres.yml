spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/accountsdb
    username: accounts
    password: accounts
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect